import React from "react";
import { Card, CardContent } from "@mui/material";

const MyComponent = () => {
  return (
    <Card>
      <CardContent>
        <h2>My Backstage Component</h2>
        <p>This is a simple component for Backstage.</p>
      </CardContent>
    </Card>
  );
};

export default MyComponent;
